name: Rebuild on push to preview

on:
  workflow_dispatch:
  push:
    branches: ["preview"]
  pull_request:
    branches: ["preview"]

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  lint-and-test-and-build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Build
        run: make build
        env:
          PATH_PREFIX: ${{ github.repository_owner != 'packagecontrol' && format('/{0}', github.event.repository.name) || '/' }}

      - name: Lint
        run: make lint

      - name: Test
        run: make test

      - name: Upload Pages artifact (push)
        if: ${{ github.event_name == 'push' }}
        uses: actions/upload-pages-artifact@v3

  and-deploy:
    if: ${{ github.event_name == 'push' }}
    needs: lint-and-test-and-build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Setup pages
        uses: actions/configure-pages@v4

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
